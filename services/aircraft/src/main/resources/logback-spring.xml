<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Suppress Micrometer noise -->
    <logger name="io.micrometer.registry.otlp" level="ERROR"/>

    <root level="INFO">
        <appender-ref ref="Console"/>
    </root>

</configuration>